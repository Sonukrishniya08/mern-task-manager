function Navbar() {
  return (
    <nav className="navbar">
      <h2>Task Manager</h2>

      <div>
        <button>Login</button>
        <button>Register</button>
      </div>
    </nav>
  );
}

export default Navbar;